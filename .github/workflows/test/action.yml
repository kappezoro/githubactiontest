#inputs:
#  TTT:
#    description: 'test TTT'
#    required: true
#    default: '1'
runs:
  using: "composite"
  steps:
    - name: Test
      run: |
          echo ${{inputs.TTT}} | sed -e 's/\(.\)/\1 /g'
          pwd
          ./test.sh
      shell: bash
