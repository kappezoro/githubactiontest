inputs:
  TTT:
    description: 'test TTT'
    required: true
    default: '1'
runs:
  using: "composite"
  steps:
    - name: Test
      run: echo "${{github.event.inputs.TTT}} !!"
      shell: bash
