#inputs:
#  TTT:
#    description: 'test TTT'
#    required: true
#    default: '1'
runs:
  using: "composite"
  steps:
    - name: Test
      run: |
          echo ${{inputs.TTT}} | sed -e 's/\(.\)/\1 /g'
          cd ${GITHUB_WORKSPACE}
          ./test.sh
      shell: bash
