#inputs:
#  TTT:
#    description: 'test TTT'
#    required: true
#    default: '1'
runs:
  using: "composite"
  steps:
    - name: Test
      run: |
          echo ${{inputs.TTT}} | sed -e 's/\(.\)/\1 /g'
          ./.github/workflows/test/test.sh
      args:
          - ${{ inputs.TTT}}
      shell: bash
