inputs:
  TTT:
    description: 'test TTT'
    required: true
    default: '1'
runs:
  using: "composite"
  steps:
    - name: Test
      run: |
          echo "${{TTT}} !!"
          echo $TTT
      shell: bash
